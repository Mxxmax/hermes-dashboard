# RemindMe — 定时提醒与任务调度

Hermes Agent 内置了强大的定时任务系统（Cron），可以在指定时间或按固定间隔自动执行任务，并将结果推送给你。

## 基本用法

### 创建提醒

你可以用自然语言创建定时任务。在对话中直接告诉 Hermes：

- "每30分钟提醒我检查一次服务器状态"
- "每天早上9点给我发送今日科技新闻"
- "5分钟后提醒我提交代码"
- "每周五下午6点提醒我写周报"

Hermes 会在后台调度这些任务，到时间自动执行。

### 通过 Dashboard 管理

在 Dashboard 的 **Cron** 页面，你可以看到所有已创建的定时任务：

| 功能 | 说明 |
|------|------|
| 查看任务列表 | 显示所有任务的名称、调度规则、状态、上次运行时间 |
| 暂停/恢复 | 临时暂停任务而不删除，稍后可恢复 |
| 手动运行 | 立即执行一次任务（不改变原定调度计划） |
| 删除任务 | 彻底移除不再需要的任务 |
| 创建任务 | 添加新的定时提醒 |

### 创建定时任务的参数

通过 Dashboard 创建任务时，支持以下配置：

- **名称** — 给任务起个名字方便识别
- **提示词** — 告诉 Hermes 到时间要做什么（例如"查询今天天气并总结"）
- **调度规则** — 支持多种格式：
  - `30m` — 每30分钟
  - `every 2h` — 每2小时
  - `0 9 * * *` — 每天9:00（标准 crontab 表达式）
  - ISO 时间戳 — 特定时间执行一次
- **重复次数** — 可选，限制任务执行的次数
- **投递目标** — 可选，指定结果发送到哪个平台/频道

## 调度规则格式

### 简单格式（推荐）

```
30m       每30分钟
45s       每45秒
2h        每2小时
every 6h  每6小时
every day 每天
every Mon 每周一
```

### Crontab 格式

```
┌───────── 分钟 (0-59)
│ ┌──────── 小时 (0-23)
│ │ ┌─────── 日 (1-31)
│ │ │ ┌────── 月 (1-12)
│ │ │ │ ┌───── 星期 (0-6, 0=周日)
│ │ │ │ │
* * * * *
```

常用示例：

```
*/5 * * * *     每5分钟
0 * * * *       每小时整点
0 9 * * *       每天早上9点
0 9,18 * * *    每天早上9点和下午6点
0 0 * * *       每天午夜
0 0 * * 1       每周一午夜
0 0 1 * *       每月1日午夜
*/30 9-18 * * * 每天9点到18点之间每30分钟
```

### 一次性提醒

使用 ISO 时间戳格式：

```
2026-06-01T09:00:00+08:00    2026年6月1日上午9点（北京时间）
2026-12-31T23:59:00Z         2026年12月31日23:59（UTC）
```

## 从对话中管理任务

你可以在聊天的过程中直接管理定时任务：

- **"停止提醒 xxx"** — 停止并删除指定名称的任务
- **"暂停提醒 xxx"** — 临时暂停任务
- **"恢复提醒 xxx"** — 恢复已暂停的任务
- **"有哪些定时任务？"** — 查看所有活跃任务
- **"立即执行 xxx"** — 手动触发一次指定任务

## 最佳实践

1. **给任务起个有意义的名字** — 方便后续管理和搜索
2. **提示词要自包含** — 定时任务执行时没有对话上下文，所以提示词需要包含完整的指令
3. **注意时区** — 默认使用系统本地时区（Asia/Shanghai）
4. **结果投递** — 任务执行结果会自动推送给你当前所在的对话

## 配置文件

在 `config.yaml` 中可以配置 cron 模块的行为：

```yaml
cron:
  wrap_response: true   # 结果是否包装头部/尾部信息，设为 false 可获取纯净输出
```

## 技术细节

- 定时任务使用 Hermes Agent 内置的调度器运行，不依赖系统 crontab
- 每个任务在独立的 agent 会话中执行，拥有完整工具集
- 任务执行结果通过消息平台投递给用户
- 任务调度使用 asyncio 事件循环，精度在秒级
- 重复任务默认无限循环，可通过 `repeat` 参数限制次数
